# Weather-App
 A Dynamic Weather App created using HTML CSS and JavaScript to display the weather data according to the city entered by user
 it uses openweather api to fetch real time weather data of any particular city entered and shows it 
 the data is visible only if the city name is valid otherwise it will display only a simple message of "invalid city name"
